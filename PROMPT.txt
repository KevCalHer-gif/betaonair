Aplica estas tres correcciones usando bloques SEARCH/REPLACE:
CORRECCIÓN 1 — src/app/(frontend)/styles.css
Agrega scroll-behavior: smooth; dentro del bloque html existente.
CORRECCIÓN 2 — src/app/(frontend)/page.tsx
Agrega id="contenido" a la sección de "Nuestros Programas". Busca esta línea exacta:
<section style={{ padding: '2rem 0' }}>
Y reemplázala por:
<section id="contenido" style={{ padding: '2rem 0' }}>
Pero solo en la primera sección que contiene el listado de programas, no en otras secciones.
CORRECCIÓN 3 — src/components/sections/HeroSection.tsx
Busca el elemento anchor o botón que navega a /#contenido y reemplázalo por un <button> con onClick que haga scroll suave:
tsx<button
  onClick={() => {
    const el = document.getElementById('contenido')
    if (el) el.scrollIntoView({ behavior: 'smooth', block: 'start' })
  }}
  style={{
    display: 'inline-block',
    backgroundColor: '#c61d4a',
    color: '#fff',
    padding: '0.75rem 2rem',
    fontWeight: 700,
    letterSpacing: '0.1em',
    textTransform: 'uppercase',
    border: 'none',
    cursor: 'pointer',
    marginTop: '1.5rem',
  }}
  onMouseEnter={(e) => (e.currentTarget.style.backgroundColor = '#e0205a')}
  onMouseLeave={(e) => (e.currentTarget.style.backgroundColor = '#c61d4a')}
>
  Ver nuestro trabajo
</button>
RESTRICCIONES:

Usar SEARCH/REPLACE, NO crear archivos nuevos
No modificar ningún otro elemento ni componente
No cambiar el texto del botón ni sus estilos más allá de lo indicado