LISANDRO: Necesito corregir un error de build de Next.js 16 relacionado con Server Components y eventos del mouse.

ERROR ACTUAL:
"Event handlers cannot be passed to Client Component props"

El problema ocurre en:
src/app/(frontend)/portafolio/page.tsx

Actualmente existe un div con:

* onMouseEnter
* onMouseLeave

El archivo es un Server Component y NO quiero convertir toda la página en 'use client'.

OBJETIVO:

* Mantener la página como Server Component.
* Eliminar completamente el uso de onMouseEnter y onMouseLeave.
* Reemplazar el efecto hover usando únicamente CSS o estilos compatibles con Server Components.
* Mantener la animación visual del borde al hacer hover.
* Mantener compatibilidad total con Next.js 16 y Vercel build.

REQUISITOS IMPORTANTES:

* NO agregar 'use client' al page.tsx.
* NO romper SSR.
* NO convertir toda la página en Client Component.
* Mantener el diseño visual actual.
* Usar una solución profesional y optimizada.

La sección problemática actual es similar a:

<div
  style={{
    background: '#0a0a0a',
    border: '1px solid #333',
    borderRadius: '8px',
    padding: '1.5rem',
    textAlign: 'center',
    transition: 'border-color 0.3s ease'
  }}
  onMouseEnter={e => e.currentTarget.style.borderColor = '#c61d4a'}
  onMouseLeave={e => e.currentTarget.style.borderColor = '#333'}
>

Quiero que reemplaces esto por una solución basada en CSS hover compatible con Server Components.

Después de los cambios:

1. ejecutar npm run build
2. verificar que no existan errores
3. asegurar compatibilidad con producción/Vercel
4. no modificar otras páginas innecesariamente
